services:
  - type: web
    name: airplanes-live-mcp
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: python sse_wrapper.py
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
